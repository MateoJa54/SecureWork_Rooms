# Base de datos PostgreSQL (Docker)
DB_USER=securework_user
DB_PASSWORD=cambiar_esto
DB_NAME=securework

# Supabase — obtener en supabase.com → Project Settings → API
SUPABASE_URL=https://tu-proyecto.supabase.co
SUPABASE_ANON_KEY=eyJ...tu_anon_key
SUPABASE_JWT_SECRET=tu_jwt_secret
SESSION_TOKEN_SECRET=s3cur3w0rk_s3ss10n_s3cr3t_2024_espe_d1str1bu1d4s

# Node
NODE_ENV=development

# Puertos (no cambiar salvo conflicto)
BACKEND_PORT=3000
FRONTEND_PORT=5173

# IP para demo en LAN — cambiar a tu IP local si otros se conectan desde otra laptop
# Windows: ipconfig → IPv4 Address
# Linux: ip addr → inet
HOST_IP=localhost